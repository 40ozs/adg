"""Versioned collector and API contracts.

Each major version is a package (`v1`, later `v2`) mirroring the published JSON Schemas
under `docs/contracts/`. A breaking change means a new major version and a migration note,
never an edit to an accepted contract.
"""
