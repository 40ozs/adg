# Phase handoffs

One file per prompt/phase, recording what was built, what was decided, what was tested with
exact results, what is deliberately incomplete, and what the next phase must assume.

These files are the contract between phases. Read the previous handoff before changing
code.
